name: Docker Image CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.RYANGOOSLING }}
          password: ${{ secrets.DOCKERTOKEN }}
      
      - name: Build and push
        uses: docker/build-push-action@v2
        with:
          context: ./
          file: ./Dockerfile
          builder: ${{ steps.buildx.outputs.name }}
          push: true
          tags: ${{ secrets.RYANGOOSLING }}/simplewhale:latest
          cache-from: type=registry,ref=${{ secrets.RYANGOOSLING }}/simplewhale:buildcache
          cache-to: type=registry,ref=${{ secrets.RYANGOOSLING }}/simplewhale:buildcache,mode=max
